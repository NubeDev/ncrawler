# grafana-triage

Placeholder skill bundle for the `ncrawler-report-ai` builder
(milestone M3). Loaded by `starter-skills::SkillRegistry` and selected
from an artifact's `source` + `tags`.

The system prompt and resource files land in a later milestone; this
file reserves the bundle directory so the skills/ layout exists from
stage 1.
